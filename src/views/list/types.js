import { string } from "prop-types";

export const defaultTypes = {
  title: undefined,
  release: undefined,
  format: undefined,
  stars: undefined
};

export const types = {
  title: string,
  release: string,
  format: string,
  stars: string
};
